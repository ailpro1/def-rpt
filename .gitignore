node_modules/
package-lock.json
*.tmp.mjs
.DS_Store

# The PWA build output, at the repo root only. worker/dist/worker.js is the
# file you paste into Cloudflare, so it is committed on purpose.
/dist/
